# Code inside of a while-true loop will repeat forever!

while True:
    # Move right
    hero.moveRight()
    # Move up
    hero.moveUp()
    # Move left
    hero.moveLeft()
    # Move down
    hero.moveDown()
    
item = pet.findNearestItem()
if item.type == "Gem":
    pet.fetch(item)
